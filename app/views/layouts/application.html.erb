<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="http://code.jquery.com/jquery-latest.js"></script>
    <title><%= content_for?(:title) ? yield(:title) : "BoomBoxin" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>


  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="#"> BoomBoxin </a>
          <div class="container nav-collapse">
            <ul class="nav">
              <li><%= link_to "Home", static_pages_index_path  %></li>
              <li><%= link_to "About Us", static_pages_new_path  %></li>
              </ul>
              <ul class="nav pull-right">
              <% if current_user %>
                <li><%= link_to "Join Party", parties_path  %></li>
                <li><%= link_to "New Party",  new_party_path  %></li>
			          <li><%= link_to "Sign Out",   user_path(current_user), :method => :delete  %></li>
              <% else %>
                <li><%= link_to "Sign In", login_path  %></li>
              <% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
      <div class="content">
        <div class="row">
              <%= content_tag :p, flash[:success], :class => "flash notice" if flash[:success] %>
              <%= content_tag :p, flash[:notice], :class => "flash notice" if flash[:notice] %>
              <%= content_tag :p, flash[:alert], :class => "flash alert" if flash[:alert] %>
              <%= yield %>
        </div><!--/row-->
      </div><!--/content-->

      <footer>
        <p>&copy; BoomBoxin 2012</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>

